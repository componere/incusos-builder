[project]
name = "incusos-builder-docs"
version = "0.0.0"
description = "MkDocs documentation site for incusos-builder."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.7",
]

[tool.uv]
package = false
