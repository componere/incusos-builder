# Technical Notes

- Use hexagonal architecture at all times. Keep business logic isolated from CLI, filesystem, network, storage, and other external adapters.
- Prefer functional testing before calling any feature complete. Unit tests are useful, but they do not prove the tool works the way the design intends.
- Take an agile approach to development. Avoid waterfall: underspecify when useful, prototype early, learn from the result, and refine from working behavior.
- Authoritative design docs: `.journal/001/ARCHITECTURE.md` (hexagonal design, ports, exit taxonomy, invariants) and `.journal/001/PLAN.md` (7-phase implementation plan). Phases 0–6 are merged (PRs #7–#9, #11–#13, #19). v0.1.0 (2026-08-17, PR #10) remains a **draft** GitHub Release, superseded by v0.2.0; publish or delete it deliberately. **v0.2.0 (2026-08-22, PRs #39–#41) is fully published on all five channels** — GitHub Release, GHCR image, Homebrew cask, Scoop manifest, and APT/DNF/APK at `https://pkgs.componere.dev` — each verified from the consumer side (session 008).
- Upstream pin: `github.com/lxc/incus-os/incus-osd@v0.0.0-20260815030500-0f5b8057f2fc` (commit; the release tag lacks `api/seed.Services`). `--version` prints it via `runtime/debug.ReadBuildInfo`. CI gate `root:check-upstream` denies `incus-osd/{internal,cmd}` in the closure.
- Upstream reference: shallow clone of `lxc/incus-os` at `reference/incus-os/` (gitignored). Spike findings live in `.journal/002/spike-1*.md`; spike code quarantined on master under `spikes/` (own go.mod each, never promoted).
- Seeding mechanic confirmed live: seed-data partition starts at exactly byte 2,148,532,224 (both arches), 100 MiB; uncompressed tar, entries mode 0600, yaml/v4 `WithV2Defaults()` byte-matches upstream `writeSeed`. GPT probed per-image (512/2048/4096 sector sizes; ISO layout puts the header at byte 2048).
- Sentinels live in `internal/errdefs` (leaf) with §6-named re-exports (`update.ErrFetch`, `config.ErrConfig`, …); exit taxonomy 0/1/2/3/4/5/6 mapped only in `internal/cli`.
- Server documents (index.json, update.json, sjson payload) are plain-decoded by design; integrity = digests + caps (64 MiB index / 1 MiB metadata) + three-way Filename/Sha256/Size binding. Plain `http://` `--server` is a usage error; the HTTPS client also rejects redirect downgrades on the final response URL.
- Rescue media: go-diskfs v1.9.4, RR-only ISO (go-diskfs Joliet output unusable), GPT+FAT32 raw at 1 MiB with proportional FAT-overhead sizing; `WriteRescue` replaces the tmpPath inode — publishers must reopen by path. go-diskfs FAT `File.Read` can return final-cluster zero slack to `io.ReadAll`; read exact logical bytes with `Stat` + `io.ReadFull`.
- Boot acceptance: **passed** 2026-08-17 on Semaphore Cloud (`f1-standard-4`, nested KVM, Incus VM with UEFI + vTPM), pipeline `4c5cc805` / job `a8b16331`, ~$0.25. Seed consumption was observed for the first time anywhere. O1–O3 were asserted against strings in the pinned upstream source; O4 (recovery payload) was accepted by the pipeline and adjudicated by the developer, because the guide forbids inventing a success criterion. Record `.journal/005/BOOT_10_RECORD.md`, raw log `.journal/005/TRACK_C_GATE_LOG.txt`, runbook `.journal/005/TRACK_C_RUNBOOK.md`. The Phase 5 mystery is solved twice over: the old gate hashed the **source** installer media, which `CleanupPostInstall` never modifies, so it could not pass on any host at any price; and plain QEMU has no `org.linuxcontainers.incus` virtio port, so the installer TUI never mirrored to `ttyS0` and a failed install looked identical to a silent one (`internal/tui/tui.go:67-71`). Still untested: `N-MEDIA-3`, the NUL-padded ISO label — the raw config matches the GPT partlabel and never reads it.
- E2E: `root:e2e` is opt-in (`INCUSOS_BUILDER_E2E=1`, `runInCI: false`) and live-proves versions parsing, smallest-image build, eleven-section seed round-trip, and exact ISO/FAT32 rescue read-back. `internal/testfixture.Generate` makes a sparse ~2.7 MB mirror for ordinary testscript coverage of exit codes 2–6, `--json`, `-o -`, and SOPS stdin. Stream large artifacts; never capture them in test memory.
- SOPS: any top-level `sops` key selects in-memory decryption; all decrypt failures map to exit 4. Throwaway age key + fixtures under `internal/config/testdata/`.
- Tooling gotchas: run lint via `mise x -- golangci-lint` (a global 2.11.4 shadows the pinned 2.12.2); moon caches can mask stale lint results; agent contexts should demand cwd verification (worktree, not main checkout) and rebuilding a package after every file write. Removing a Worktrunk worktree leaves a stale golangci-lint cache that fails the next `root:lint` from a fresh worktree with `can't read file … no such file`; `golangci-lint cache clean` clears it. This recurred three times in one session, so treat it as expected rather than a new defect.
- Release plumbing: `incusos-builder` releases through the reusable **meigma/release** unit, pinned everywhere (every `uses:`, every `checksum-signing-workflow-ref`, the `componere/pkgs` policy, and its local receiver) to one SHA — currently `0dee66ff6c4cc7e28d7bb65e97a37d701e0eff4a` (v0.1.17). A mixed revision is unsupported; bumps update all pins in one reviewed change and re-diff the `componere/pkgs` local receiver against upstream. The App is now slug `componere-release` (same integration id `4551177`, so tag bypass is unchanged), installed on exactly `incusos-builder`, `pkgs`, `homebrew-tap`, `scoop-bucket`. Credentials are **org-level**, selected-visibility: var `COMPONERE_RELEASE_APP_CLIENT_ID` + secret `COMPONERE_RELEASE_APP_PRIVATE_KEY` (incusos-builder only), five `MACOS_*` secrets (componere-scoped Developer ID + ASC key, 1Password `Development/Apple Componere`), and producer `RPM_/APK_SIGNING_*` repo secrets. Bespoke `release.yml`/`attest.yml`/`release-dry-run.yml` are retired.
- Native packages: `componere/pkgs` owns policy + public keys; aggregate and producer signing keys live in 1Password `Development/Componere Packages Aggregate Signing Keys` and `…incusos-builder Package Signing Keys`; R2 bucket `componere-packages` behind `pkgs.componere.dev`. **GitHub does not deliver caller environment secrets to a reusable workflow owned by another org** (fails as silently-empty, `secrets: inherit` cannot reach env scope), so the receiver is a **local** workflow selecting `packages-production` — documented upstream in meigma/release#68; scaffold feature tracked in #67. Key formats are load-bearing: producer APK = traditional RSA PEM with legacy `Proc-Type: 4,ENCRYPTED` (nfpm uses `x509.DecryptPEMBlock`), aggregate APK = **unencrypted** PEM (`abuild-sign`), both OpenPGP keys armored+passphrased; `openssl pkcs12` needs `-legacy` for Keychain exports.
- GoReleaser gotchas: `--skip=sign` also strips nfpm RPM/APK package signatures, not just Cosign steps; `gomod.proxy: true` fetches the module from the Go proxy at the tag, so local stage validation of an unpushed tag is impossible without flipping proxy off. melange rejects `vendor`/`homepage`/`maintainer` under `package:` (the maintained meigma/release example sets them and cannot build). melange packages the staged GoReleaser binary (`application`); reintroducing `go/build` would ship a second, unattested binary.
- Release versioning: the manifest seeded `0.1.1` from `meigma/template-go` — the template's own release state, not ours — which made Release Please propose `0.1.2` from releases that never happened. Corrected in PR #26. Two non-obvious mechanics, both read from release-please source: `manifest.ts` excludes exactly `"0.0.0"` from the manifest backfill, so a `0.0.0` manifest means *no previous release* and `base.ts buildNewVersion()` returns `initialReleaseVersion()` — which defaults to **1.0.0** unless `initial-version` is set; and `bump-patch-for-minor-pre-major` makes features bump the patch below 1.0, which would have frozen the project at 0.1.x forever. Current config: manifest `0.0.0`, `initial-version: "0.1.0"`, `bump-minor-pre-major: true` (breaking → minor, stays in 0.x), `bump-patch-for-minor-pre-major: false`. `CHANGELOG.md` must stay **empty**, not a `# Changelog` stub: the updater runs `adjustHeaders()` over pre-existing content when no version heading exists and re-appends the demoted stub as a stray `## Changelog` (PR #29).
- (The manifest now reads `0.2.0` after the v0.2.0 release; the mechanics above still govern behavior below 1.0.)
- Verifying releases: `gh` 2.97.0 prints **nothing** on a successful `gh attestation verify`, so a bare exit 0 is not evidence. Pair every check with a deliberate falsifier and read the failure text. Since v0.2.0 the signer identities are the **shared meigma/release workflows**, so verification needs `--signer-workflow`: assets = `meigma/release/.github/workflows/publish-github-release.yml`, image = `…/publish-oci-image.yml`, checksum bundle identity = `https://github.com/meigma/release/.github/workflows/go-pre-publish.yml@<release-unit-sha>`; always `--repo componere/incusos-builder`.
- User docs follow Diátaxis under `docs/docs/{tutorials,how-to,reference,explanation}`. Root repository docs are current, and source is dual-licensed under `Apache-2.0 OR MIT`.
- `.github/repository-settings.toml` was applied 2026-08-17 with `uv run .github/scripts/configure_github_repo.py apply` (`plan` first, then `apply`). Live now: private vulnerability reporting, automated security fixes, immutable releases, and managed branch/tag rulesets; both rulesets carry an always-on `RepositoryRole 5` admin bypass, and tags additionally bypass for the release App (integration `4551177`, slug now `componere-release`). Session 008 cut the required branch checks to `ci` + `GitHub Pages` after the dry-run workflows were deleted (a ruleset requiring checks from a deleted workflow blocks every PR); note `apply` once failed with `PATCH …/rulesets 404` and the ruleset was converged with a direct `PUT`. Nine manifest keys remain manual GitHub UI follow-ups.
